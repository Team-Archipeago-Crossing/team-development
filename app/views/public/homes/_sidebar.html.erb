<div class="sidebar col-3 bg-warning p-5">
  <%# フラッシュメッセージ見えないように表示しています ※トップ画面に表示しないと他のviewにログイン関係のメッセージが表示されてしまうため%>
  <p class="opacity-0"><%= flash[:notice] %></p>
  <table class="text-center mx-auto border border-dark">
    <thead class="border border-dark">
     <tr>
        <th>ジャンル検索</th>
      </tr>
    </thead>
    <tbody>
      <% Genre.all.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, genre_path(genre.id) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>